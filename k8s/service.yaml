apiVersion: v1
kind: Service
metadata:
  name: gen-sp-service
  namespace: gen-sp
  labels:
    app: gen-sp
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: gen-sp
